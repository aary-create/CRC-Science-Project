# Optional. Without it, users and community reports aren't saved between
# requests (they still work within a session). Location search, hospitals
# and the map need no key at all — they run on free, keyless OpenStreetMap
# services (Nominatim + Overpass), not Google.
DATABASE_URL=
