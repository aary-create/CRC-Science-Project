# Optional but recommended. Powers the location search box, "use my
# location", the map, and nearest-hospital search on the Help page. In
# Google Cloud Console enable BOTH "Maps JavaScript API" and "Places API".
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Optional. Without it, users and community reports aren't saved between
# requests (they still work within a session).
DATABASE_URL=
